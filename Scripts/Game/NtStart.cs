using UnityEngine;
using System.Collections;
using UnityEngine.Networking;

public class NtStart : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //GetComponent<NetworkManagerHUD>().manager.StartMatchMaker();
    }
	
	// Update is called once per frame
	void Update () {
        //GetComponent<NetworkManagerHUD>().manager.StartMatchMaker();
    }
}
